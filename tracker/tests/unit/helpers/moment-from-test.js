import { momentFrom } from 'tracker/helpers/moment-from';
import { module, test } from 'qunit';

module('Unit | Helper | moment from');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = momentFrom([42]);
  assert.ok(result);
});
